# 逆向网易云音乐



```python
# 网易云抓包
1. 进入音乐界面, 点击播放, 抓包获取请求
2. 找到发送请求的函数, 往前或往后找加密逻辑
3. 找到上一个函数, 如果数据被加密继续往前找
	chrome  Call Stack可以往前找,直到找到未加密的数据位置
    分析 数据是怎么加密的, 
    	把它原本的代码用python实现
    	如果有网站自己写的加密方法, 那么可以 用console 判断一下是否 是固定的值, 如果是就不用担心算法问题了, 可以直接忽略算法, 直接使用固定的值
        可控的值+可控的值= 固定的值, 这个值对面服务器可以解析
        
```

"eval"破解

```javascript
eval(
    function(){}(参数)
)

// 给函数其起名字
function fn(){代码}

fn("参数") //这就破解完毕了
```



